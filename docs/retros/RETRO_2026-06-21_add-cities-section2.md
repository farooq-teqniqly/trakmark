# Retro — add-cities, Section 2: Domain: CityId — 2026-06-21

## Findings applied

| Evidence | Improvement | File(s) changed |
|----------|-------------|-----------------|
| pr-reviewer agent reported it "could not run dotnet build/test because Bash tool access was denied," despite the worktree's `settings.local.json` correctly allow-listing `Bash(git *)` and `Bash(dotnet *)`. The orchestrator's own prompt template told the reviewer to "Run from within the worktree directory" without specifying invocation syntax — a likely `cd <path> && dotnet ...` compound command does not start with `dotnet`/`git` and therefore does not match either allow-list pattern, causing an outright denial rather than a build/test failure. | Documented the exact failure mode and required invocation style (`git -C <path> ...` / `dotnet test <path>\...`, never `cd <path> && ...`) directly in `pr-reviewer.md`'s own instructions (so the agent is self-sufficient regardless of how it is prompted) and in the orchestrator's reviewer prompt template. Also instructed the reviewer to retry in the corrected form before falling back to static-only review. | `.claude/agents/pr-reviewer.md`, `.claude/agents/trakmark-engineering-team.md` |
| `Run-Coverage.ps1` failed under Windows PowerShell 5.1 (`$PSVersionTable.PSVersion` confirms 5.1.26100 is the only `powershell.exe` available in this environment) with a "positional parameter cannot be found" error. Root cause confirmed by reading the script: `Join-Path $repoRoot "Trakmark" "Trakmark.slnx"` and the four-argument `Join-Path` call for `$OutputDir` both use the PowerShell 7+ multi-segment overload, which PS 5.1 does not support. The `coverage-report` SKILL.md's own bootstrap snippet (`Join-Path $repoRoot ".claude" "skills" "coverage-report"`) had the identical bug. This would break the mandatory 100%-domain-coverage pre-merge gate for every future section until fixed. | Rewrote both `Join-Path` call sites in `Run-Coverage.ps1` and the bootstrap snippet in `SKILL.md` to chain two-argument `Join-Path` calls, which is PS 5.1-compatible, with a comment explaining why. | `.claude/skills/coverage-report/Run-Coverage.ps1`, `.claude/skills/coverage-report/SKILL.md` |
| Orchestrator's own Bash `cd <repo-root>` (Step 5 merge) and `cd <worktree> && ...` (Step 3 fix-commit fallback) patterns are denied when the target path is outside the orchestrator's own cwd — reproduced directly in this retro session (`cd /c/src/my/trakmark && git log ...` was denied; `git -C C:/src/my/trakmark log ...` succeeded). This was previously undocumented, forcing every orchestration run to rediscover the workaround by trial and error. | Replaced all `cd <path> && ...` examples in the merge step and fix-commit fallback with `-C <path>` / absolute-path-first-arg forms, and added an explicit guardrail bullet stating the `cd` restriction and the required workaround pattern. | `.claude/agents/trakmark-engineering-team.md` |

## Carry-forward (not yet actionable)

- The pr-reviewer's actual Bash denial in this cycle was not directly observed by the orchestrator (it ran inside an isolated worktree subagent), so the `cd`-vs-allow-list-pattern explanation is the most likely root cause based on the orchestrator's own reproducible `cd` denial, but it has not been confirmed by re-running the same reviewer prompt with the corrected invocation style. Worth re-validating on the next section that triggers a fix→re-review cycle requiring an actual `dotnet test` run inside a worktree.
- `git worktree remove --force` hit a transient "Permission denied" on first attempt before succeeding on retry (likely a file lock from a recently-exited process holding the worktree directory open). No rule change applied — this looks like a one-off OS-level timing issue rather than a systemic gap; flag for a rule only if it recurs across multiple cycles.
